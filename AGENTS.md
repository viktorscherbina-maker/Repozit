## Cursor Cloud specific instructions

This is an empty test repository ("Тестовый репозиторий") containing only a `.gitkeep` placeholder file. There are no application services, dependencies, or build steps to run.

- **No package manager or dependencies** are configured.
- **No lint, test, or build commands** exist yet.
- **No services** need to be started.

When code is added to this repository, update this section with relevant dev environment instructions (how to install dependencies, run tests, start services, etc.).
